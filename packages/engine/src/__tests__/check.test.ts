// Consolidated into per-module test files:
// - Profile tests → check/profiles.test.ts
// - Baseline I/O + delta tests → check/baseline.test.ts
// TODO: Delete this file
import { describe, it } from "vitest";
describe("check (consolidated)", () => {
  it.skip("tests moved to per-module files", () => {});
});
